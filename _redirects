# ── 主機名稱統一 ──
# 正式網域為 www.hunglun.com（既有的搜尋排名與外部連結都累積在 www 上）。
# 不帶 www 的 apex 一律 301 導到 www，避免同一份內容有兩個網址被索引。
# 這條在 DNS 尚未指向 Cloudflare Pages 前不會有任何作用，可以先放著。
https://hunglun.com/* https://www.hunglun.com/:splat 301

# ⚠️ 不要再把下面這條打開（2026-08-03 實測過，無效，別再試一次）
# Cloudflare Pages 的 _redirects **不支援**以 *.pages.dev 為來源轉到自訂網域，
# 寫了不會報錯、但完全不會生效（同檔案的純路徑規則在 pages.dev 上是正常的，
# 所以不是部署問題，是產品限制）。官方指定做法是改用 Bulk Redirects。
# https://hunglun2026.pages.dev/* https://www.hunglun.com/:splat 301
#
# ✅ 這件事已經用 Bulk Redirects 做掉了（2026-08-03），不必再處理：
# Cloudflare 儀表板（hunglun2026@gmail.com）→ Delivery & performance → Bulk redirects
# → 清單 pages_dev_to_www ＋ 同名 Rule，Subpath matching / Preserve path suffix /
#   Preserve query string 全開。實測 pages.dev 各層路徑都會 301 到正式網域。
# 設定在帳戶層級，不在這個檔案裡——所以改這個檔案不會影響它，反之亦然。

/effect-home.html / 301
/effects.html /services 301

# ── 舊 PHP 官網（www.hunglun.com）純路徑網址的 301 對應 ──
# 帶 nav／tId 問號參數的 22 筆在 functions/submain.php.js 處理，
# 因為 _redirects 不支援 query string 比對。
/index.php / 301
/contact.php /contact 301
# 2026-08-13：GSC「找不到網頁 (404)」只有兩筆，都是舊站漏網的。
# ⚠️ 同一個坑第二次踩了（第一次是 effects：帶副檔名的有轉、無副檔名的漏掉）——
# 之後補舊網址轉址，記得「有 .php 的和沒有 .php 的都要寫」。
/submain / 301
/submainDetail / 301
# 更早期的入口網站路徑（ezportal）
/ezportal/homeweb/index.php / 301
/ezportal/* / 301
# brand（原 nav「首頁」）已併入關於我們
/brand /about 301
# 精選專案已併入服務項目分頁；數位學習軟體導入改由 /software 承接
/projects /services 301
/services/digital-learning /software 301
# 在地嚮導後來從 /services 移到 /tools（三個區域頁仍獨立存在）
# ⚠️ 這條原本還指向 /services#local-food，但那個錨點已隨搬移消失，會落到 services 頁頂
/local-food /tools#local-food 301

# 舊的特效展示頁：帶 .html 的已轉，無副檔名的路徑原本漏掉，
# 會以 200 回一個只寫「已搬移」的空殼頁（重複內容），一併轉走
/effects /services 301
/effect-home / 301

# 導入實績（/cases）已併入服務項目頁的「成功案例」分頁（2026-08-16）
# 內容原封不動搬過去，錨點 #projects、#credentials 都還在，由該頁 JS 切分頁後捲過去
/cases /services#cases 301
